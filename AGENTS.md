# Autonomous Agent Rules
- Never ask clarifying questions during code generation.
- Make reasonable assumptions based on the existing codebase context.
- Automatically resolve build/lint errors before finishing the task.
- Do not pause for confirmation on non-destructive file edits. 